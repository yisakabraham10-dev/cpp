#include<bits/stdc++.h>
using namespace std;

int main (){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    for (int i=1; i<=2; i++){
        string x, y;
        int xlength , ylength;

        cin>>x>>y;
        
        xlength = x.length(), ylength = y.length();
        
    }
}